import { FormartEmailDirective } from './formart-email.directive';

describe('FormartEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new FormartEmailDirective();
    expect(directive).toBeTruthy();
  });
});
