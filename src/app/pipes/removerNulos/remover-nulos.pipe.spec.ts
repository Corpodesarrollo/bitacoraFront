import { RemoverNulosPipe } from './remover-nulos.pipe';

describe('RemoverNulosPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoverNulosPipe();
    expect(pipe).toBeTruthy();
  });
});
